public String nameFromEmail(String email) {
    List<String> breakOnAt = String.split(email, '@');
    List<String> fullName = String.split(breakOnAt, '.');

    return fullName.get(0).toLowerCase().capitalize() + ' '+fullName.get(2).toLowerCase().capitalize();
}